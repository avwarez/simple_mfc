// afximpl.h — simple_mfc.
// In real MFC this is an internal implementation header, not meant for
// direct application #include. Some projects (e.g. eMule/srchybrid's
// Stdafx.h, an old AppWizard carryover) #include it anyway without
// referencing any symbol from it. An empty pass-through satisfies that
// include without dragging in the real Visual Studio-installed MFC
// internals, which would redefine CObject/CWnd/etc. on top of
// simple_mfc's own declarations.
#pragma once
