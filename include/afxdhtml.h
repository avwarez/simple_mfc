// afxdhtml.h — reference STUB (declarations only, no implementation).
// CDHtmlDialog (a dialog hosting a DHTML/IE control). Used by eMule/
// srchybrid only as a base class in the class hierarchy — zero methods
// of it are actually called (see ../../mfc_scan_srchybrid.md), so no
// method signature to transcribe here, only the hierarchy.
#pragma once
#include "afxwin.h"

// ---------------------------------------------------------------------
// CDHtmlDialog (header afxdhtml.h, hierarchy
// CObject -> CCmdTarget -> CWnd -> CDialog -> CDHtmlDialog)
// ---------------------------------------------------------------------
class CDHtmlDialog : public CDialog {};
